[pytest]
pythonpath = . tests
markers =
    live_railway: hits deployed Railway (set RAILWAY_BASE_URL); slow, uses real GPU/CPU inference
    live_ai_e2e: local llama (LLAMA_URL) + Groq; set RUN_LIVE_AI_E2E=1 and GROQ_API_KEY
