[pytest]
pythonpath = . tests
markers =
    live_railway: hits deployed Railway (set RAILWAY_BASE_URL); slow, real Groq vision calls
    live_ai_e2e: live Groq vision + recipes E2E; set RUN_LIVE_AI_E2E=1 and GROQ_API_KEY
